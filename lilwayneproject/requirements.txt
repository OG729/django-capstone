Django==3.2.5  # Django web framework
django-crispy-forms==1.12.0  # For easy form styling
django-widget-tweaks==1.4.11  # For additional control in templates

# Authentication and Authorization
django-allauth==0.45.0  # Integrated set of Django applications addressing authentication, registration, account management
django-rest-auth==0.9.5  # Provides a set of REST API endpoints for handling authentication and registration

# Database
# Note: Make sure to use the appropriate database driver based on your database choice (e.g., psycopg2 for PostgreSQL, mysqlclient for MySQL)
psycopg2==2.9.1  # PostgreSQL adapter for Python
# mysqlclient==2.1.0  # MySQL adapter for Python (uncomment if you're using MySQL)

# Polls
django-polls==1.2.1  # A basic Django app to conduct Web-based polls

# Additional libraries (add more if you have specific requirements)
# Example: Pillow for handling images, django-filter for filtering querysets
# pillow==9.0.0
# django-filter==2.5.0

